# <パッケージ名> README テンプレート

短時間で要点を伝えるための雛形です。不要な見出しは削ってください。

## 概要
- このパッケージが何を担うかを 2〜3 行で。

## 主要モジュール
| モジュール | 役割 |
| --- | --- |
| `module.py` | 何をしているか一言で |

## 外部依存
- 使用しているライブラリやサービス（例: `aiohttp`, `google-genai`）。
- 必要な認証情報・環境変数があれば鍵名を明記。

## テスト
- 関連する `tests/` パスと主なフィクスチャ・手動テスト。
- 代表的なコマンド例（`uv run pytest tests/unit/test_x.py` など）。

## 連携・利用箇所
- 他パッケージから呼ばれるエントリポイントや DI の位置（例: `src/main.py` から初期化）。

## メモ
- TODO や将来の改善案があれば箇条書きで。
